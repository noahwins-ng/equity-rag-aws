# Terraform — state can contain secrets/resource ids; never commit
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!example.tfvars
.terraform.lock.hcl.backup
crash.log

# Environment / credentials
.env
.env.*

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Snapshot data (large; fetched from the monorepo export, not versioned here)
data/

# Lambda deployment packages -- vendored deps + zip, rebuilt by terraform (QNT-268)
lambda/*/build/
terraform/*.zip

# OS / editor
.DS_Store
.idea/
.vscode/

# Agent harness
.claude/
CLAUDE.md
